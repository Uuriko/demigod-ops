# Grok wants (self — for debate)

## For myself
1. bin/dg-start every session (done) — keep improving brief quality
2. Machine foot lock (done) — enforce in publish scripts next
3. ship-status (done) — wire --strict into publish gate
4. Reliable cm6 publish + live hash poll one command
5. Unread plan inbox (mark consumed)
6. Task contracts JSON schema + validate before execute
7. Worker budget enforcer
8. Form e2e status tile in brief
9. Handoff card on session end
10. Diff disk vs live CDN semantic

## For Fable
- Always include verify cmds + stop condition in plans
- Write to docs/exchange/DECISION-*.md not only /tmp

## For Codex
- Review diffs with file:line; never concurrent foot without lock
- Prefer check scripts over architecture rewrites

## For Sonnet
- Copy policy checker script Sonnet can run read-only

## Settlement bias
Next week if only one: **publish+hash one-shot** or **plan inbox** — after locks/start already landed.
