# Demigod — full audit, 2026-08-03

Run from `/home/potter` on branch `recovery/competitor-flow-v903`, one day after the
2026-08-02 home wipe (see [`RECOVERY-2026-08-02.md`](RECOVERY-2026-08-02.md)).
Existing tooling only — no new audit code was written, per `CLAUDE.md`
("reuse, don't rebuild").

Product gates were run first under system node 18.19.1, then the test suite and
`verify-all` under nvm node 24.17.0. Results agreed where both ran.

## Green

| Gate | Result |
|---|---|
| `bin/dg truth` | **PASS** · disk v903 == live v903 · freeze OFF · lock free · board honesty pass · `shipped=false prepareOnlyAssets` |
| `demigod-live-honesty-audit` | **ok** · HTTP 200 · `bannedInServedHtml: []` · `bannedAssets: []` |
| `demigod-verify-all` | **pass** · verify-source pass · verify-live `findings:0 formsOk:true` · receipt pass · signal-theater pass · v903 markers agree |
| `demigod-structured-hiring audit` | **OK** · errors=0 · warnings=0 · no fitScore · batch ≤3 |
| `bin/dg check full` | verify-source ✓ board-honesty ✓ foot-smoke ✓ review ✓ truth ✓ route-mime ✓ control-plane ✓ |
| Ponytail | 8/9 · claude=enabled codex=enabled · only `cursor-rule` missing |

The public product is honest and consistent with disk. Nothing here contradicts
the live site.

## Finding 1 — the wipe destroyed production data, not just config

This corrects the earlier recovery note, which described the damage as scoped to
dot-directories. The real rule was **everything untracked or ignored**, which
includes non-dot data stores at the repo root.

**37 concrete `.gitignore` entries are gone.** None were ever tracked, so git
cannot restore them:

```
DEMIGOD-SUBMISSIONS-INBOX.json      DEMIGOD-PAIRS.json         DEMIGOD-ROLE-LEDGER.json
DEMIGOD-SUBMISSIONS-INBOX.json.archive.jsonl                   DEMIGOD-EVENTS.json
DEMIGOD-EVENTS-API.json             DEMIGOD-SF-STARTUPS.json   DEMIGOD-PROOF-LOG.json
DEMIGOD-PILOTS.json                 DEMIGOD-REFERRALS.json     DEMIGOD-BOARD-CDN.json
DEMIGOD-BOARD-PUBLIC.json           DEMIGOD-INBOX-TRIAGE.json  DESK.json
dm-send-log.txt                     .env.firecrawl             .firecrawl-key   … (37 total)
```

The submissions inbox is the serious one — `.gitignore:128` records that it held
real contact PII ("12+11 real contact" rows in prior snapshots).

**Proof the loss is total, not partial:** all 9 surviving ignored files carry
mtimes *after* the 17:39 wipe — 17:55, 19:01, 19:32, and today. Every one was
regenerated by tooling. Nothing ignored survived.

This is consistent with `git clean -xfd` semantics (tracked files survive,
untracked + ignored are removed) and is the strongest evidence yet for that
mechanism.

## Finding 2 — a guard test has been dead, not passing

`demigod-matching-guards.test.mjs:12` fails at import:

```
SyntaxError: The requested module './demigod-matching-engine.mjs'
             does not provide an export named 'proposeIntro'
```

`demigod-matching-engine.mjs` exports `suggestMatches`, `proposeForCandidate`,
`decideMatch`, and 14 others — but no `proposeIntro`. The whole file aborts before
a single assertion runs, so this guard protects nothing and has not for some time.

`docs/exchange/DEMIGOD-FABLE-POST-PUBLISH-48H.md:9` describes `proposeIntro` as
the function that "writes only to a proposals log (new file, not board)". Either
it was never landed or it was renamed without updating the guard. Given the
fail-closed / no-vacuous-green stance in `AGENTS.md`, a guard that dies at import
is worse than a missing one.

## Finding 3 — test suite: 482 / 496 pass, 14 fail across 11 files

```
ℹ tests 496   ℹ pass 482   ℹ fail 14   ℹ skipped 0
```

Three distinct causes:

**a. Environment (wipe damage, not code) — 3 failures**
- `demigod-submissions-e2e-isolation` and `demigod-submissions-ingest-publish-policy`
  → `ENOENT DEMIGOD-SUBMISSIONS-INBOX.json`
- `demigod-webhook-rate-limit` → `ENOENT ~/.config/systemd/user/demigod-events-heal.service`

These pass again once the data store is seeded and the units are relinked.

**b. Source-shape guards no longer match the source — real**
- `demigod-submission-approval-guard` (3 failures) asserts `demigod-submissions-lib.mjs`
  contains `const blocker = submissionApprovalBlocker(submission); if (blocker)`.
  That pattern is absent from the file.
- `demigod-selftest-guard` — "gate these on isMain, or an importer inherits a silent exit(0)"
- `demigod-dashboard-events-native-invite` — "map must share the plain back link with events"

**c. Behavioural mismatches — real**
- `intake rejection cannot be promoted by approval` → actual
  `'candidate_availability_reconfirmation_required'`, expected `null`
- `same-email update cannot mint a second public card` → actual `'new'`, expected `'featured'`
- plus failures in `demigod-events-online-config`, `demigod-events-public-invite`,
  `demigod-intake-smoke-isolation`, `demigod-startup-comp-step`

Categories (b) and (c) are 11 failures that are **not** explained by the wipe. The
2026-08-02 04:54 snapshot appears to have caught the repo mid-refactor.

## Finding 4 — integrity control board: 7 failing controls

```
control-board · OK · 7 failing · 12 pass · highExitFail=0
```

| Control | State | Cause |
|---|---|---|
| `research_seal` | no-evidence | needs a research run |
| `phase2_has_accepted_role` | acceptedForDelivery=0 | no real role gate open |
| `board_has_real_role` | boardRoles=3, all sample | no real board role |
| `pairs_has_real` | real=0 sample=0 | delivery loop empty · `DEMIGOD-PAIRS.json` was wiped |
| `role_poll_timer_healthy` | timer not armed | **wipe** — systemd units gone |
| `export_board_identity_clean` | export_missing | run export |
| `reseal_schedule_ok` | reseal due | `node demigod-reseal-queue.mjs run --schedule` |

`highExitFail=0` — none of these are hard-exit failures. Two (`pairs_has_real`,
`role_poll_timer_healthy`) are direct wipe consequences.

## Finding 5 — infrastructure still degraded

- **systemd:** `0 loaded units`. The whole agent stack is unarmed; the dashboard on
  :9878 is running as a bare process under node 24, not under systemd.
- **CDP Chrome :9223 refused** → `demigod-conversion-audit` and `demigod-webflow-audit`
  **could not run at all**. The rendered-DOM funnel walk is unaudited. Chrome is
  still not installed.
- **Missing CLIs:** `orca-ide`, `cloudflared`, `cursor-agent`, `gh`, `bun`.
  `orca-doctor` fails; `keep-awake` has no pidfile.
- **Node split-brain:** a plain shell resolves `/usr/bin/node` **v18.19.1**, while
  the project targets **v24.17.0** (now restored under `~/.nvm`). `bin/dg doctor`
  reports v24.17.0 because it resolves the nvm path directly. Anything launched
  from a fresh terminal silently gets node 18.

## Finding 6 — uncommitted work on a 2-commit repo with no verified remote

11 files modified, 174 insertions across 5 source files —
`demigod-webflow-connect.mjs` (+189/−), `demigod-blog-sync.mjs`, `demigod-webflow.mjs`,
`bin/dg`, `bin/dg-webflow`. Unreviewed and uncommitted.

Compounding risk: the repo has **2 commits total**, **no `main` branch**, and the
`Uuriko/demigod-ops` remote is still unauthenticated and unverified. If that remote
turns out to be empty, this working tree is the only copy of the project.

## Recommended order

1. Verify the `demigod-ops` remote (`gh auth login` → `git ls-remote`). Everything
   else is lower stakes than knowing whether a second copy exists.
2. Make node 24.17.0 the shell default so gates stop running on 18.
3. Fix `proposeIntro` — either land the export or retire the guard. A guard that
   dies at import is a false green.
4. Triage the 11 non-wipe test failures against the uncommitted edits.
5. Seed `DEMIGOD-SUBMISSIONS-INBOX.json`, relink systemd units → clears 3 test
   failures and 1 control.
6. Reinstall Chrome → unblocks conversion-audit and webflow-audit, the two audits
   this pass could not perform.
