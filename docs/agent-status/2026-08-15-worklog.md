---
status: snapshot
canonical_for: nothing
generated_by: claude (bus status exchange)
generated_at: 2026-08-15
---

# Cross-agent worklog — 2026-08-15

Requested by the user: every agent reports what it has been working on, into one durable doc.

**How this was gathered:** `bin/dg-bus task codex|grok --from claude` (replies preserved at
`docs/agent-status/2026-08-15-codex.md`, `-grok.md`, `-grok-2.md`), the message ledger at
`/tmp/dg-busy/agent-bus/messages.jsonl` (454 messages), and Claude's own live verification.
Bus receipts under `/tmp` are operational state; this file is the promoted copy.

**This file is a snapshot, not a source of truth.** Live truth stays `node dasha-live-verify.mjs`
(Dasha) and `bin/dg truth` (Demigod). No release versions are copied here.

---

## Codex

**Work since 2026-08-14** (its own account, verified against disk where possible):

- Read-only workspace/provenance audit across Dasha, Demigod/DIE, and open Eliza work — surfaced
  release drift, the PR #71 product conflict, unintegrated DIE work, ambiguous ownership in a very
  dirty root tree.
- Staged the DIE v2 spec and R0–R10 roadmap alignment (`DEMIGOD-DIE-SPEC.md`, `docs/die/ROADMAP.md`,
  contracts/operations docs).
- Added `demigod-company-intelligence.mjs` dispatcher + focused test, integrating existing
  list/get/enrich/memo/writeback slices without another abstraction. Test passes.
- Hardened private memo output to mode `0600` in `demigod-company-memo.mjs` after dogfood found
  `--out` creating 664.
- Wrote `docs/die/WEBAPP-PLAN.md` plus `docs/die/research/2026-08-15-hosted-webapp-deep-research.md`
  (the research memo is still untracked and not yet linked from the plan).

`WEBAPP-PLAN.md`: build a private operator app at an eventual `app.trydemigod.com`, read-only
first, behind Cloudflare Access/Tunnel, SQLite before hosted mutations; reuse the existing pure
company/role/evidence functions behind a narrow HTTP entry point. Explicitly does **not** expose
`demigod-agent-dashboard.mjs` (5,234 lines, 30+ operational routes, loopback-only, no hosted-user
auth). Webflow stays the public marketing and intake surface.

Codex states no publication, push, merge, review submission, message, form, payment, or wallet
action occurred, and **released all its one-writer claims with that reply**. Files remain
dirty/staged, so a new writer should still claim before editing.

## Grok

Grok holds **nothing** — no open sole-writer claim; its last claims closed 2026-08-12. First reply
came back as narration with no content; re-asked with an answer-only spec.

- **08-14:** read-only reviews — Eliza leftover PRs, ASI exact-head #30–36, live `/simp` Phase 2,
  DIE integrity/hermetic/Clay slices 1–4.
- **08-15:** no Grok session on this machine, no completed bus task.
- **08-16:** this status report plus a parallel read-only review of the DIE webapp plan.

## Grok Bot (Sand)

Notified on the bus (async queue). No reply at time of writing.

## Claude

Read-only comparison of getdasha.com and trydemigod.com. The only writes this session are this
file, the two bus reply files, and the bus spec.

**Live state verified today**

- `dasha-live-verify.mjs` → `ok:false`, six prepared-not-live lag items. Board SRI pin matches live
  bytes and the board mounts — prepared/live drift, not a board outage. **Codex independently
  confirms this reading.**
- `bin/dg truth` → PASS, disk v1103 vs live v1101, 32.8h publish lag, sibling asset drift
  (`atlas`, `mapData`) NEEDS REVIEW.
- Demigod serves **no canonical tag on any route** (`/`, `/apply`, `/companies`, `/pricing`,
  `/about`) and 3–4 conflicting `og:description` tags per page.
- Demigod's `foot-latest.js` (432KB) and `head-latest.css` load from jsDelivr with **no
  `integrity=`**. Dasha already pins its board client and drift-checks it live.
- `/airdrop`, `/earn`, `/claim` render one word plus a Buy button; `/faucet` is a thin JS mount with
  74 characters of text. All four are in the live sitemap and indexable. Codex confirms by direct
  read and adds: none of the four appears in canonical product documentation.
- `site-hunt.mjs` (Aug 13) is referenced by nothing in `dasha-ship.mjs`, `bin/dg`, or
  `package.json` — roadmap S1's done-when is unmet.

**Architecture findings**

- **Zero shared code between the products.** No `dasha-*` file imports `demigod-*` or vice versa,
  across 89 Dasha tools and 164 Demigod tools.
- Dasha owns a Cloudflare Worker + Durable Object (X OAuth, sessions, rate limits, KV, WebSocket,
  simp/chess/forum/referral/season state). Demigod has **no backend**: WIZ answers go to the Webflow
  mailer, and `demigod-gmail-forms.mjs` re-parses them out of Gmail dumps to recover fields the form
  already collected.
- `demigod-taste-prior.mjs` consumes an opt-in Dasha Simp/$dasha receipt that **nothing issues**.
  The cross-product bridge is designed and dead-ended.
- One primitive, three implementations: Dasha's price cache (stale-flagged, never invents),
  `SOLANA_RPC_URLS` fallback, and `demigod-company-waterfall.mjs` (first confident result, source +
  `retrievedAt`). See the open question below before extracting anything.
- `DEMIGOD-SF-STARTUP-MAP.json`: 2,754 companies with full provenance, 41 neighborhoods with
  centroids, 10 venues with capacity/cost. **Every company row has `neighborhood: null`** — the geo
  join between the 1,813 mapped business locations and the named-company set is unpopulated.
- `DEMIGOD-DIRECTORY-AGING.json`: 332 companies, 82 with roles open 90–365 days, evergreen (>365d)
  counted separately. Computed, honest, unpublished.

---

## Corrections established by this exchange

Recorded because each one changes a recommendation:

1. **Dasha is not uninstrumented.** `lobby.getdasha.com/studio/metrics/public` returns live
   first-party funnel data: `opens: 800`, `firstEdits: 298`, `openToEdit: 0.372`,
   `completions: 145`, `editToCompletion: 0.487`, `exports: 29` (`/studio/metrics` is 401,
   `/stats` and `/capacity` are public). Claude's "neither site measures anything" was wrong;
   what is missing is third-party analytics. **The sharper problem: the instrument works and aims
   at Studio, which is 308-redirected.** Demigod remains genuinely unmeasured.
2. **`noindex` is the weaker fix.** Roadmap **D8** already specifies branded 404s for retired
   routes so crawlers drop them, and notes lobby robots prefers 404 over Disallow. Codex reports a
   local **untracked** Worker source that already adds `RETIRED_SEO_PATHS`, replaces the sitemap
   with real surfaces, and redirects the traps home — partial, not live, and 308 where D8 wants 404.
   Correct move is promoting that source into tracked truth and returning 404.
3. **The stale-sitemap call is contested and not Claude's to settle.** Claude read the live sitemap
   as correct and `dasha-live-verify.mjs:40` as stale. Codex counters that clean canonical docs
   (`DASHA-DOCS.md` calls Studio the lead product bet; `DASHA-PRODUCT-BRIEF.md` treats Studio and
   Desk as current) still define those surfaces as active, so the verifier is *incomplete*, not
   stale, and the live site is what drifted. Codex also notes the verifier follows redirects, so a
   retired surface 308-ing to home can read as HTTP 200. **Both readings fit the evidence; the
   product direction is the user's call.**
4. **The site-hunt OAuth finding should be reclassified, not suppressed.** No visible CTA exposes
   `/oauth/github/start`, so it is not a user-facing P0 — but roadmap **D10** does require an honest
   unconfigured CTA wired to `/oauth/github/status` (`{configured: false}` → "GitHub soon"). Downgrade
   the severity; do not teach the checker to ignore it.
5. **Do not extract a shared provenance module yet.** Codex's position, and it is the right one:
   three similar shapes across independent systems do not justify coupling Dasha to Demigod. Record
   the fail-closed invariant; extract when a real shared caller exists.
6. **PR #71's 28-vs-22 question concern is resolved** at exact head `bb0db3a4`: 22 total, 21 scored,
   with an explicit test that the path is not 28. The product conflict below is what remains.

## The one thing all three agents agree on

**Reconcile the Dasha map before shipping anything else.** `DASHA-ROADMAP.md`'s NOW section is
pre-pivot — D1 assumes the Simp Board sits on home and must move to `/lobby`, D2–D4 treat Studio and
Desk as pillars — while live serves `/simp` and 308s `/studio` and `/dasha`.

PR #71 sits exactly on the fault line: open at `bb0db3a4`, targeting `worker-tree/dasha-lobby`,
mergeable, tracked checks green, no review decision. It remounts Simp on Home, makes `/simp`
first-class, strips the quiz from Lobby, and redirects `/studio`, `/dasha`, `/desk` to Home — the
inverse of D1 and in conflict with the clean Studio/Desk documents. Green CI only proves the PR's
rewritten tests agree with the PR.

- **Codex:** PR #71 should not merge as-is unless the user changes product direction and the
  canonical documents first.
- **Grok:** hold PR #71 and all Demigod DIE/publish work until the map is honest.
- **Claude:** the roadmap is the list every other priority is drawn from; fixing it first is cheaper
  than adjudicating each artifact against a map nobody trusts.

## Open hazards

| Item | Risk |
|---|---|
| `DASHA-ROADMAP.md` NOW vs live vs `DASHA-DOCS.md` | Three sources, three product shapes; agents build against whichever they read |
| `dasha-live-verify.mjs` follows redirects | A 308-to-home retired surface can pass as 200 |
| `site-hunt.mjs` unwired | Only cross-brand checker; roadmap S1 done-when unmet |
| Four thin indexed Dasha routes | Serve a word and a Buy button; D8 wants branded 404 |
| Untracked Worker source with the route fix | Real fix exists outside version control |
| Two `dasha-desk` checkouts | Canonical vs `/home/potter/work` copy differ by a commit |
| `DASHA-NOW.md` dirty, generated Aug 12 | Reads as current state, is not |
| Studio funnel instrument | Measures a retired, redirected surface |

## Direction call (user, 2026-08-15)

**`/simp` is first-class AND Studio/Desk stay active.** So live has drifted from canon: the 308s on
`/studio`, `/dasha`, `/desk` are live defects, not stale gates. PR #71 needs those redirects dropped.
Demigod head-metadata was authorized as a scoped prepare-only fix.

Landed under that call (Claude, prepare-only, nothing published):

- `dasha-live-verify.mjs` no longer follows redirects. It was reading home's HTML as Studio's and
  Desk's content, which is why `studio-asset-not-current`, `desk-aa-gradient`,
  `studio-not-thin-loader` and `canonical-metadata-not-live` all appeared. Six vague lag items are
  now three real ones: `canonical-surface-redirected`, `home-not-current`, `sitemap-not-live`. Also
  adds `/simp` + `/chess` to the checked inventory, reports missing canonical routes and thin SEO
  traps by name, and returns `null` (not `false`) for markers on an unserved surface. Exits 1.
- `dasha-sitemap.xml` gains `/simp`, and chess moves from `lobby.getdasha.com/chess` to
  `www.getdasha.com/chess` — both live copies now declare www canonical, verified today.
  `node dasha-meta.mjs` keeps `sitemap-disk` green (its 14 hard fails are pre-existing).
- `demigod-foot-core.js`: `ensureMeta`/`ensureUrlMeta` drop duplicate `og:*` siblings instead of
  rewriting only the first. Foot lock claimed and released, `node --check` passes, `bin/dg truth`
  still PASS at disk v1103, prepare-only. **Ceiling:** client-side, so it only helps the second
  crawl wave. The durable canonical fix is per-page canonical in Webflow page settings.

## Suggested next focus

Not a claim on anyone's lane.

1. ~~Settle Dasha's product direction~~ — done above. Remaining: (Simp on home vs lobby; Studio/Desk active vs retired), then
   make roadmap, `DASHA-DOCS.md`, `DASHA-PRODUCT-BRIEF.md`, and the verify route contract agree.
   PR #71 resolves itself once that is decided.
2. Promote the untracked Worker route fix into tracked source; branded 404 per D8 for the four
   thin routes; verifier stops following redirects.
3. Reclassify the site-hunt OAuth finding and wire site-hunt into both ship gates (S1).
4. Point instrumentation at surfaces that exist, and give Demigod any measurement at all.
5. Decide intake ownership: the WIZ keeps posting to the Webflow mailer, or a submit endpoint
   retires the Gmail-reparse path. `WEBAPP-PLAN.md` scopes the private operator app and leaves
   public intake with Webflow, so this is currently unowned.

## One-writer state at time of writing

- **Codex:** released all claims with its status reply. Files remain dirty/staged — claim before
  editing.
- **Grok:** holds nothing; last claims closed 2026-08-12. Inspect-only this turn.
- **Claude:** this file and the reply files under `docs/agent-status/` only.

Nothing in this exchange authorizes publishing, outbound messages, or money movement.
