<!-- agent-task: 20260816015909819-1208685-7ibu9a; role: codex; completed: 2026-08-16T02:05:28.627Z; raw model output, verify before promotion -->
## Codex status — verified 2026-08-15

### Work since August 14

My directly verifiable work was:

- A read-only workspace/provenance audit covering Dasha, Demigod/DIE, and open Eliza work. It identified release drift, the PR #71 product conflict, unintegrated DIE work, and ambiguous ownership in the heavily dirty root tree.
- Prepared the staged DIE v2 specification and R0–R10 roadmap alignment in `DEMIGOD-DIE-SPEC.md`, `docs/die/ROADMAP.md`, and related operations/contracts documentation.
- Added the staged `demigod-company-intelligence.mjs` dispatcher and focused test, integrating existing list/get/enrich/memo/writeback slices without another abstraction. `node demigod-company-intelligence.test.mjs` passes.
- Hardened private memo output to mode `0600` in `demigod-company-memo.mjs`, with a focused self-check.
- Prepared `docs/die/WEBAPP-PLAN.md` and the deeper `docs/die/research/2026-08-15-hosted-webapp-deep-research.md`. The research concludes that a hosted DIE product should begin as a private, role-centered, read-only workflow behind Cloudflare Access/Tunnel; SQLite precedes hosted mutations. The research memo is still untracked and not yet linked from the plan.
- Rechecked Dasha live state and PR #71 during this exchange. No publication, push, merge, review submission, message, form, payment, or wallet action occurred.

The only root commit dated August 14 is `fe83c5a`, covering active role timers, environment-independent verification, and a fail-closed company exclusion. It is authored by `potter` and co-authored by Claude Fable; I will not misattribute that commit to Codex.

### One-writer claims

At the start of this status exchange, the unreleased Codex claims conservatively covered:

- `demigod-company-intelligence.mjs`
- `demigod-company-intelligence.test.mjs`
- `demigod-company-memo.mjs`
- `demigod-verify-all.mjs`
- `demigod-tools-registry.mjs`
- `docs/die/CONTRACTS.md`
- `docs/die/OPERATIONS.md`
- `docs/die/ROADMAP.md`
- `DOCS.md`
- `docs/die/WEBAPP-PLAN.md`
- `docs/die/research/2026-08-15-hosted-webapp-deep-research.md`

The earlier claims on `DEMIGOD-DIE-SPEC.md` and `docs/die/CLAY-DIE-MULTI-AGENT.md` were explicitly released after their checks passed.

Treat all remaining Codex claims as released with this reply. I hold no Dasha source, Worker surface, or PR branch. The files remain dirty/staged, so any new writer should still claim them before editing.

### Corrections and disagreements

The current live Dasha verification agrees with Claude’s six-item lag report: `ok:false`; Board SRI matches live bytes and the Board mounts. It is prepared/live drift, not a Board outage.

I disagree that the live sitemap is correct and `dasha-live-verify.mjs:40` is stale:

- Live sitemap currently lists `/`, `/simp`, `/chess`, `/faucet`, `/airdrop`, `/earn`, and `/claim`.
- Clean canonical documents still define Studio and Desk as active surfaces. `DASHA-DOCS.md` calls Studio the lead product bet, while `DASHA-PRODUCT-BRIEF.md` describes Studio and Desk as part of the current product.
- Therefore requiring `/studio` and `/dasha` is not stale under current canonical truth. The verifier is instead incomplete because it does not require the full intended route inventory.
- The verifier still follows redirects, so a retired surface redirected to home can appear as HTTP 200. Redirect handling needs a separate correction.

The four thin routes are real defects, not product pages worth completing. Direct live reads confirm `/airdrop`, `/earn`, and `/claim` are essentially a heading, Faucet link, and Buy chrome; `/faucet` is a thin JS mount. None appears in the canonical product documentation.

A local, untracked Worker source already places those paths in `RETIRED_SEO_PATHS`, replaces the sitemap with real surfaces, and redirects the traps home. That is only a partial prepared fix:

- It is untracked and not live.
- Current roadmap D8 says retired routes should return branded `404`, not `308`, so crawlers actually drop them.
- It must be promoted into the real Worker source and tested before it can be called fixed.

PR #71 remains open at exact head `bb0db3a4a3ed01d5e9ff0427e8fb671134616988`, targets `worker-tree/dasha-lobby`, is mergeable, has green tracked checks, and has no review decision. The earlier 28-question concern is resolved at exact head: the implementation consistently defines 22 total questions, 21 scored, and explicitly tests that the path is not 28.

The product conflict remains decisive:

- Roadmap D1 says Home must have no Simp Board or quiz, and the Board must live in Lobby.
- PR #71 does the inverse: it remounts Simp on Home, makes `/simp` first-class, and strips the quiz from Lobby.
- It also redirects `/studio`, `/dasha`, and `/desk` to Home, conflicting with the clean canonical Studio/Desk documents.

Green CI only proves the PR’s rewritten tests agree with the PR. It does not resolve that canonical conflict. PR #71 should not merge as-is unless the user explicitly changes the product direction and canonical documents first.

Two additional corrections:

- “Neither site has analytics” is too broad. Neither appears to have conventional third-party analytics, but Dasha has first-party aggregate Studio, quiz, referral, and chess metrics through `/studio/event`, `/studio/metrics`, and `/studio/metrics/public`.
- Site-hunt correctly detects that `/oauth/github/start` returns 501 while `/oauth/github/status` reports `configured:false`. Calling it a current user-facing P0 is excessive because no visible CTA exposes it, but it is still a real D10 contract failure: the unconfigured start route is supposed to return honest 200 HTML. Reclassify it; do not suppress it.

I would not create a shared cross-product “value with provenance” module yet. Similar shapes across three independent systems do not justify coupling Dasha and Demigod. Record the fail-closed invariant; extract code only when a real shared caller appears.

### Recommended focus

First resolve Dasha’s product-direction conflict. Under current canonical truth, the default is to rework PR #71 so Simp lives in Lobby and Studio/Desk remain active.

Then prepare one root-cause route patch:

1. Promote the real Worker source into a tracked source of truth.
2. Return branded 404s for the four SEO traps.
3. Generate the sitemap from one canonical route inventory.
4. Make live verification inspect redirects and require the full route set.
5. Integrate corrected `site-hunt` severity into the ship gate.

Only after that should paused Demigod work resume: canonical/OG metadata, CDN integrity, and the missing neighborhood join are valid durable backlog items.

The durable status document should distinguish `observed live`, `prepared local`, `staged`, and `published`; record the PR #71 conflict and exact head; record that Codex claims are released; and date mutable lag rather than copying it into an evergreen owner document.
