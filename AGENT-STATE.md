# Demigod — Current State

Canonical living state: [`DEMIGOD-COMPRESSED-STATE.md`](DEMIGOD-COMPRESSED-STATE.md).

Use `bin/dg orient` for current NEXT and `bin/dg truth` for fresh release evidence. This pointer exists because some agent clients still open `AGENT-STATE.md` directly.
