Reviewed disk state directly (not just docs): the prepared checkpoint already covers the three known drift items (Home headline/nav, Desk trust-reset, Studio formats) — `DASHA-DOCS.md` confirms these are verified-prepared, not something still to build. The actual blocker is Webflow auth (API `401`, Designer serves an empty stub, dashboard forces human verification).

One thing worth flagging before the task split: there are 26 versioned `cdp-v20.mjs` … `cdp-post-v30.mjs` scripts in the tree, all using `chromium.connectOverCDP` against port 9223 to try to drive the Webflow Designer programmatically. `DASHA-DOMAIN-WEBFLOW-LAUNCH.md` explicitly says *"do not automate around the human-verification boundary."* The empty-document response Designer gives back looks like anti-automation detection, which is consistent with why 26 near-identical attempts all failed. This line of work should stop — it's fighting the platform's own guard, and 26 retries suggest diminishing returns, not a solvable puzzle.

**1) Grok:** Stop the CDP-automation attempts against Designer. Delete the 26 dead `cdp-*.mjs` scripts, `.tmp-dasha-ship/`, and the stray empty `Dasha` file — pure clutter from that dead end. Hand the actual re-login to Potter (human-in-the-loop, one real browser session), and in the meantime assemble the exact publish payload (page IDs + three HtmlEmbed source blocks + SEO fields already in `DASHA-DOMAIN-WEBFLOW-LAUNCH.md`) so publish is copy-paste-click once auth returns.

**2) Claude:** Run the full pre-publish gate (`dasha-desk/build.mjs --check`, `dasha-desk.test.mjs`, `dasha-landing.test.mjs`, `dasha-social-card.test.mjs`, `dasha-meme-studio.test.mjs`, `dasha-relay-lab.test.mjs`) to confirm every "prepared" checkpoint still actually passes — this is the only way to know the payload Grok assembles is trustworthy, and it needs zero Webflow access.

**3) Codex:** Finish the `/how-to-buy` page prep — verify `dasha-how-to-buy.html` against its own build/test gate and produce the exact HtmlEmbed + nav-wiring diff, so creating the Webflow page is a single paste once a page ID exists. Don't link it from Home/Desk yet (doc is explicit: 404 until the route is proven live).

**4) Risk if wrong:** If CDP automation against Designer continues, repeated headless hits against a human-verification wall risk getting the Webflow session/account flagged or locked — turning a temporary auth inconvenience into a real lockout, which is strictly worse than the current "blocked" state. Separately, if Claude or Codex re-touch the headline/Desk/Studio copy without checking `DASHA-DOCS.md` first, that's wasted work — those are already verified-prepared, not open items.
