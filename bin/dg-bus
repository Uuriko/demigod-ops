#!/usr/bin/env bash
# dg-bus — local task bus (Claude ⇄ Grok ⇄ Codex)
# See demigod-agent-bus.mjs and AGENT-COMMS.md
set -euo pipefail
export PATH="${HOME}/.local/bin:${HOME}/bin:/usr/bin:/bin:${PATH}"
ROOT="${DEMIGOD_ROOT:-/home/potter}"
exec node "${ROOT}/demigod-agent-bus.mjs" "$@"
