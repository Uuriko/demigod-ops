#!/usr/bin/env bash
# OBSOLETE (2026-08-08). Dasha ships via Webflow + dasha-ship.mjs, not catbox/Thesis.
# Kept as a hard stop so agents never re-upload landing to catbox or gate on conviction receipts.
set -euo pipefail
echo "ABORT: bin/dasha-publish is retired."
echo "Use:  node dasha-ship.mjs --ship"
echo "  or: npm run dasha:ship"
echo "Do not upload dasha-landing.html to catbox; do not run conviction-receipt gates for publish."
exit 2
